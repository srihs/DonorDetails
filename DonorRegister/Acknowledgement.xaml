<Controls:MetroWindow x:Class="DonorRegister.Acknowledgement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:DonorRegister"
        mc:Ignorable="d"
        Title="පුණ්‍යානු මෝදනා"  WindowStartupLocation="CenterScreen" IsMaxRestoreButtonEnabled="False" BorderThickness="1" BorderBrush="Black">
    <Border Padding="10">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Calendar x:Name="dtpStartDate" Margin="0,0,0,-134" Grid.Column="0" Grid.ColumnSpan="2" DisplayMode="Year" >
                    <Calendar.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="Text" Value="ආරම්භක දිනය"/>
                        </Style>
                    </Calendar.Resources>
                </Calendar>
                <Button Grid.Column="1" Margin="0,10,285,0"></Button>
            </Grid>

        </StackPanel>
    </Border>


</Controls:MetroWindow>
